name: test_on_push

on:
  push

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: tests
        uses: paolorechia/pox@v1.0.1
        with:
          tox_env: env
      - run: ls        
      

